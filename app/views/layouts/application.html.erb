<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Portal Monemusic" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <!-- Inline CSS fallback for unified styling -->
    <style>
      /* Tailwind-inspired utility classes */
      .min-h-screen { min-height: 100vh; }
      .flex { display: flex; }
      .items-center { align-items: center; }
      .justify-center { justify-content: center; }
      .bg-gray-50 { background-color: #f9fafb; }
      .py-12 { padding-top: 3rem; padding-bottom: 3rem; }
      .px-4 { padding-left: 1rem; padding-right: 1rem; }
      .px-6 { padding-left: 1.5rem; padding-right: 1.5rem; }
      .px-8 { padding-left: 2rem; padding-right: 2rem; }
      .max-w-md { max-width: 28rem; }
      .w-full { width: 100%; }
      .space-y-8 > * + * { margin-top: 2rem; }
      .space-y-6 > * + * { margin-top: 1.5rem; }
      .space-y-4 > * + * { margin-top: 1rem; }
      .mt-6 { margin-top: 1.5rem; }
      .mt-8 { margin-top: 2rem; }
      .mt-1 { margin-top: 0.25rem; }
      .mt-2 { margin-top: 0.5rem; }
      .mb-2 { margin-bottom: 0.5rem; }
      .mb-3 { margin-bottom: 0.75rem; }
      .mb-4 { margin-bottom: 1rem; }
      .mb-6 { margin-bottom: 1.5rem; }
      .text-center { text-align: center; }
      .text-3xl { font-size: 1.875rem; line-height: 2.25rem; }
      .font-extrabold { font-weight: 800; }
      .text-gray-900 { color: #111827; }
      .text-sm { font-size: 0.875rem; line-height: 1.25rem; }
      .text-xs { font-size: 0.75rem; line-height: 1rem; }
      .text-gray-600 { color: #4b5563; }
      .text-gray-500 { color: #6b7280; }
      .text-gray-700 { color: #374151; }
      .text-blue-600 { color: #2563eb; }
      .text-blue-800 { color: #1e40af; }
      .text-green-600 { color: #16a34a; }
      .text-red-600 { color: #dc2626; }
      .text-red-700 { color: #b91c1c; }
      .text-indigo-600 { color: #4f46e5; }
      .text-white { color: #ffffff; }
      .bg-red-50 { background-color: #fef2f2; }
      .bg-blue-50 { background-color: #eff6ff; }
      .bg-blue-100 { background-color: #dbeafe; }
      .bg-blue-600 { background-color: #2563eb; }
      .bg-indigo-600 { background-color: #4f46e5; }
      .bg-white { background-color: #ffffff; }
      .border { border-width: 1px; }
      .border-gray-300 { border-color: #d1d5db; }
      .border-red-400 { border-color: #f87171; }
      .border-blue-200 { border-color: #bfdbfe; }
      .border-transparent { border-color: transparent; }
      .rounded { border-radius: 0.25rem; }
      .rounded-md { border-radius: 0.375rem; }
      .rounded-lg { border-radius: 0.5rem; }
      .rounded-none { border-radius: 0; }
      .rounded-t-md { border-top-left-radius: 0.375rem; border-top-right-radius: 0.375rem; }
      .rounded-b-md { border-bottom-left-radius: 0.375rem; border-bottom-right-radius: 0.375rem; }
      .shadow { box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06); }
      .shadow-sm { box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05); }
      .-space-y-px > * + * { margin-top: -1px; }
      .appearance-none { appearance: none; }
      .relative { position: relative; }
      .block { display: block; }
      .py-2 { padding-top: 0.5rem; padding-bottom: 0.5rem; }
      .py-3 { padding-top: 0.75rem; padding-bottom: 0.75rem; }
      .px-3 { padding-left: 0.75rem; padding-right: 0.75rem; }
      .px-4 { padding-left: 1rem; padding-right: 1rem; }
      .placeholder-gray-500::placeholder { color: #6b7280; }
      .focus\:outline-none:focus { outline: 2px solid transparent; outline-offset: 2px; }
      .focus\:ring-indigo-500:focus { --tw-ring-color: #6366f1; }
      .focus\:border-indigo-500:focus { --tw-border-opacity: 1; border-color: rgba(99, 102, 241, var(--tw-border-opacity)); }
      .focus\:z-10:focus { z-index: 10; }
      .group { /* group parent */ }
      .justify-center { justify-content: center; }
      .font-medium { font-weight: 500; }
      .cursor-pointer { cursor: pointer; }
      .hover\:bg-indigo-700:hover { background-color: #4338ca; }
      .hover\:text-indigo-500:hover { color: #6366f1; }
      .hover\:text-gray-800:hover { color: #1f2937; }
      .focus\:ring-2:focus { --tw-ring-shadow: var(--tw-ring-inset, ) 0 0 0 calc(2px + var(--tw-ring-offset-width)) var(--tw-ring-color); }
      .focus\:ring-offset-2:focus { --tw-ring-offset-width: 2px; }
      .sr-only { position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border-width: 0; }
      .gap-2 { gap: 0.5rem; }
      .flex-1 { flex: 1 1 0%; }
      .inline-flex { display: inline-flex; }
      .justify-between { justify-content: space-between; }
      .ml-4 { margin-left: 1rem; }
      
      /* Base styles */
      * { box-sizing: border-box; }
      body { 
        font-family: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif; 
        line-height: 1.5;
        margin: 0;
        padding: 0;
      }
      
      main { padding: 1rem; }
      
      /* Form elements */
      input, textarea, select { 
        font-family: inherit;
        font-size: 0.875rem;
      }
      
      button, input[type="submit"] {
        font-family: inherit;
      }
      
      /* Links */
      a { text-decoration: none; }
      a:hover { text-decoration: none; }
      
      /* Alert styles */
      .alert {
        padding: 0.75rem 1rem;
        border-radius: 0.5rem;
        margin-bottom: 1rem;
      }
      
      @media (max-width: 480px) {
        body {
          font-size: 14px;
        }
        
        .text-3xl {
          font-size: 1.5rem;
          line-height: 2rem;
        }
        
        .text-sm {
          font-size: 0.75rem;
          line-height: 1rem;
        }
        
        .text-xs {
          font-size: 0.7rem;
          line-height: 0.9rem;
        }
        
        input, textarea, select {
          font-size: 14px;
        }
        
        button, input[type="submit"] {
          font-size: 14px;
        }
      }
      
      @media (min-width: 640px) {
        .sm\:px-6 { padding-left: 1.5rem; padding-right: 1.5rem; }
        .sm\:text-sm { font-size: 0.875rem; line-height: 1.25rem; }
        .sm\:inline { display: inline; }
      }
      
      @media (min-width: 1024px) {
        .lg\:px-8 { padding-left: 2rem; padding-right: 2rem; }
      }
    </style>
  </head>

  <body>
    <!-- Flash Messages -->
    <% if flash[:notice].present? || flash[:alert].present? %>
      <div id="flash-messages" style="position: fixed; top: 20px; right: 20px; z-index: 9999; max-width: 400px;">
        <% if flash[:notice] %>
          <div class="flash-notice" style="background-color: #10b981; color: white; padding: 12px 16px; border-radius: 8px; margin-bottom: 10px; box-shadow: 0 4px 6px rgba(0,0,0,0.1);">
            <%= flash[:notice] %>
          </div>
        <% end %>
        <% if flash[:alert] %>
          <div class="flash-alert" style="background-color: #ef4444; color: white; padding: 12px 16px; border-radius: 8px; margin-bottom: 10px; box-shadow: 0 4px 6px rgba(0,0,0,0.1);">
            <%= flash[:alert] %>
          </div>
        <% end %>
      </div>
      
      <script>
        // 플래시 메시지를 3초 후 자동으로 제거
        document.addEventListener('DOMContentLoaded', function() {
          setTimeout(function() {
            var flashMessages = document.getElementById('flash-messages');
            if (flashMessages) {
              flashMessages.style.transition = 'opacity 0.5s';
              flashMessages.style.opacity = '0';
              setTimeout(function() {
                flashMessages.remove();
              }, 500);
            }
          }, 3000);
        });
        
        // 페이지 로드 시 즉시 플래시 메시지 클리어 (브라우저 캐시 방지)
        if (window.performance && window.performance.navigation.type === window.performance.navigation.TYPE_BACK_FORWARD) {
          var flashMessages = document.getElementById('flash-messages');
          if (flashMessages) {
            flashMessages.remove();
          }
        }
      </script>
    <% end %>
    
    <% if controller_name == 'dashboard' && controller.class.name.start_with?('Admin::') %>
      <%= yield %>
    <% else %>
      <main>
        <%= yield %>
      </main>
    <% end %>
  </body>
</html>
