<div class="min-h-screen bg-gray-50">
  <div class="max-w-7xl mx-auto py-8 px-4 sm:px-6 lg:px-8">
    <!-- 헤더 -->
    <div class="mb-8">
      <div class="flex items-center justify-between">
        <div>
          <h1 class="text-3xl font-bold text-gray-900">예약 관리</h1>
        </div>
      </div>
    </div>
    
    <!-- 서비스 선택 탭 -->
    <div class="bg-white rounded-lg shadow mb-4">
      <div class="border-b border-gray-200">
        <nav class="-mb-px flex gap-4" aria-label="Tabs">
          <div>
            <a href="/admin/reservations?service=practice"
               class="<%= params[:service] != 'makeup' && params[:service] != 'pitch' ? 'border-violet-500 text-violet-600' : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300' %> whitespace-nowrap py-4 px-6 border-b-2 font-medium text-sm inline-block">
              연습실 예약
            </a>
          </div>
          <div>
            <a href="/admin/reservations?service=makeup"
               class="<%= params[:service] == 'makeup' ? 'border-violet-500 text-violet-600' : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300' %> whitespace-nowrap py-4 px-6 border-b-2 font-medium text-sm inline-block">
              보충수업 예약
            </a>
          </div>
          <div>
            <a href="/admin/reservations?service=pitch"
               class="<%= params[:service] == 'pitch' ? 'border-violet-500 text-violet-600' : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300' %> whitespace-nowrap py-4 px-6 border-b-2 font-medium text-sm inline-block">
              음정수업 예약
            </a>
          </div>
        </nav>
      </div>
    </div>
    
    <!-- 필터 섹션 -->
    <div class="bg-white rounded-lg p-6 shadow mb-6">
      <form action="/admin/reservations" method="get" class="flex flex-wrap gap-4">
        <input type="hidden" name="service" value="<%= @service_type || 'practice' %>">
        <div>
          <input type="text" 
                 name="search" 
                 value="<%= params[:search] %>"
                 placeholder="이름/아이디 검색"
                 class="px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-violet-500 focus:border-violet-500">
        </div>
        
        <div>
          <select name="status" class="px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-violet-500 focus:border-violet-500">
            <option value="">전체 상태</option>
            <option value="active" <%= 'selected' if params[:status] == 'active' %>>이용 전</option>
            <option value="completed" <%= 'selected' if params[:status] == 'completed' %>>완료</option>
            <option value="cancelled" <%= 'selected' if params[:status] == 'cancelled' %>>취소</option>
            <option value="no_show" <%= 'selected' if params[:status] == 'no_show' %>>노쇼</option>
          </select>
        </div>
        
        <div>
          <input type="date" 
                 name="date" 
                 value="<%= params[:date] %>"
                 class="px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-violet-500 focus:border-violet-500">
        </div>
        
        <button type="submit" style="background-color: #7c3aed; color: white; padding: 8px 32px; border-radius: 8px; font-weight: 500; border: none; cursor: pointer;" onmouseover="this.style.backgroundColor='#6d28d9'" onmouseout="this.style.backgroundColor='#7c3aed'">
          검색
        </button>
        
        <% if params[:search].present? || params[:status].present? || params[:date].present? %>
          <a href="/admin/reservations?service=<%= @service_type || 'practice' %>" class="bg-gray-600 text-white px-6 py-2 rounded-lg hover:bg-gray-700 font-medium">
            초기화
          </a>
        <% end %>
      </form>
    </div>
    
    <!-- 예약 목록 -->
    <!-- Debug: service_type=<%= @service_type %>, count=<%= @reservations&.count %>, class=<%= @reservations&.class %> -->
    <% if @service_type == 'makeup' %>
      <!-- Makeup DB test: <%= MakeupReservation.count %> reservations -->
    <% end %>
    <div class="bg-white rounded-lg shadow overflow-hidden">
      <% if @reservations && @reservations.any? %>
        <table class="w-full">
          <thead class="bg-gray-50">
            <tr>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">이름</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">아이디</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"><%= @service_type == 'makeup' ? '수업' : '연습실' %></th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">날짜</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">시간</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">상태</th>
              <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">관리</th>
            </tr>
          </thead>
          <tbody class="bg-white divide-y divide-gray-200">
            <% @reservations.each do |reservation| %>
              <tr class="<%= 'bg-gray-50' if reservation.end_time && reservation.end_time < DateTime.current %>">
                <td class="px-6 py-4 whitespace-nowrap">
                  <div class="text-sm font-medium text-gray-900"><%= reservation.user.name %></div>
                </td>
                <td class="px-6 py-4 whitespace-nowrap">
                  <div class="text-sm text-gray-500"><%= reservation.user.username %></div>
                </td>
                <td class="px-6 py-4 whitespace-nowrap">
                  <div class="text-sm text-gray-900">
                    <% if @service_type == 'makeup' %>
                      <%= reservation.subject if reservation.respond_to?(:subject) %> 보충수업
                      <div class="text-xs text-gray-500"><%= reservation.user.teacher if reservation.user.respond_to?(:teacher) %></div>
                    <% else %>
                      <%= reservation.room.number %>번
                      <% if reservation.room.respond_to?(:has_outlet) && reservation.room.has_outlet %>
                        <span title="콘센트 있음">🔌</span>
                      <% end %>
                    <% end %>
                  </div>
                </td>
                <td class="px-6 py-4 whitespace-nowrap">
                  <div class="text-sm text-gray-900">
                    <%= reservation.start_time.strftime('%Y-%m-%d') %>
                  </div>
                </td>
                <td class="px-6 py-4 whitespace-nowrap">
                  <div class="text-sm text-gray-900">
                    <%= reservation.start_time.strftime('%H:%M') %> - 
                    <%= reservation.end_time.strftime('%H:%M') %>
                  </div>
                </td>
                <td class="px-6 py-4 whitespace-nowrap">
                  <% case reservation.status %>
                  <% when 'active' %>
                    <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-green-100 text-green-800">
                      이용 전
                    </span>
                  <% when 'completed' %>
                    <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-blue-100 text-blue-800">
                      완료
                    </span>
                  <% when 'cancelled' %>
                    <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-yellow-100 text-yellow-800">
                      취소
                    </span>
                  <% when 'no_show' %>
                    <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-red-100 text-red-800">
                      노쇼
                    </span>
                  <% else %>
                    <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-gray-100 text-gray-800">
                      <%= reservation.status %>
                    </span>
                  <% end %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm">
                  <div class="flex gap-2">
                    <% if @service_type == 'makeup' %>
                      <% if reservation.status == 'pending' %>
                        <button onclick="updateReservationStatus(<%= reservation.id %>, 'active', 'makeup')" 
                                style="background-color: #16a34a !important; color: white !important; padding: 4px 12px; border-radius: 4px; font-weight: 500; border: none; cursor: pointer;">
                          승인
                        </button>
                        <button onclick="updateReservationStatus(<%= reservation.id %>, 'cancelled', 'makeup')" 
                                style="background-color: #dc2626 !important; color: white !important; padding: 4px 12px; border-radius: 4px; font-weight: 500; border: none; cursor: pointer;">
                          거절
                        </button>
                      <% end %>
                    <% end %>
                    <select onchange="updateReservationStatus(<%= reservation.id %>, this.value, '<%= @service_type || 'practice' %>')" 
                            class="text-sm px-2 py-1 border border-gray-300 rounded focus:ring-2 focus:ring-violet-500 focus:border-violet-500">
                      <option value="pending" <%= 'selected' if reservation.status == 'pending' %>>대기</option>
                      <option value="active" <%= 'selected' if reservation.status == 'active' %>>활성</option>
                      <option value="completed" <%= 'selected' if reservation.status == 'completed' %>>완료</option>
                      <option value="cancelled" <%= 'selected' if reservation.status == 'cancelled' %>>취소</option>
                      <option value="no_show" <%= 'selected' if reservation.status == 'no_show' %>>노쇼</option>
                    </select>
                    <% if @service_type == 'makeup' %>
                      <button onclick="deleteReservation(<%= reservation.id %>, 'makeup')" 
                              style="color: #6b7280; padding: 4px 12px; font-size: 0.875rem; cursor: pointer; background: none; border: 1px solid #d1d5db; border-radius: 4px;"
                              onmouseover="this.style.color='#dc2626'; this.style.borderColor='#dc2626';"
                              onmouseout="this.style.color='#6b7280'; this.style.borderColor='#d1d5db';">
                        삭제
                      </button>
                    <% end %>
                  </div>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      <% else %>
        <div class="text-center py-12">
          <p class="text-gray-500">예약이 없습니다.</p>
        </div>
      <% end %>
    </div>
    
    <% if @reservations && @reservations.any? %>
      <div class="mt-6">
        <p class="text-sm text-gray-600">
          총 <%= @reservations.length %>개의 예약
        </p>
      </div>
    <% end %>
  </div>
</div>

<script>
function updateReservationStatus(reservationId, status, service) {
  fetch(`/admin/reservations/${reservationId}/update_status`, {
    method: 'PATCH',
    headers: {
      'X-CSRF-Token': document.querySelector('[name="csrf-token"]').content,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({ status: status, service: service })
  }).then(response => {
    if (response.ok || response.redirected) {
      window.location.reload();
    } else {
      alert('상태 변경에 실패했습니다.');
    }
  });
}

function deleteReservation(reservationId, service) {
  if (confirm('정말로 이 예약을 삭제하시겠습니까?')) {
    // URL에 서비스 타입 추가
    const url = `/admin/reservations/${reservationId}?service=${service}`;
    
    fetch(url, {
      method: 'DELETE',
      headers: {
        'X-CSRF-Token': document.querySelector('[name="csrf-token"]').content,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({ service: service })
    }).then(response => {
      if (response.redirected) {
        window.location.href = response.url;
      } else if (response.ok) {
        // 페이지 새로고침
        location.reload();
      } else {
        alert('삭제에 실패했습니다.');
      }
    });
  }
}

function applyReservationFilters() {
  // 현재 폼을 제출
  document.querySelector('form[action="/admin/reservations"]').submit();
}

function resetReservationFilters() {
  const currentService = new URLSearchParams(window.location.search).get('service') || 'practice';
  window.location.href = `/admin/reservations?service=${currentService}`;
}

</script>