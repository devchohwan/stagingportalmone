<div class="min-h-screen bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-4xl mx-auto">
    <!-- 상단 타이틀 -->
    <div class="text-center mb-8">
      <div class="flex items-center justify-center space-x-3 mb-4">
        <div class="w-12 h-12 bg-violet-600 rounded-xl flex items-center justify-center">
          <span class="text-white text-2xl">⚙️</span>
        </div>
        <h1 class="text-3xl font-bold text-gray-900">계정 설정</h1>
      </div>
      <p class="text-gray-600">계정 정보를 관리하고 보안을 강화하세요</p>
    </div>
    
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
      <!-- 비밀번호 변경 카드 -->
      <div class="bg-white p-8 rounded-xl shadow-lg">
        <div class="mb-6">
          <h2 class="text-xl font-bold text-gray-900 flex items-center">
            <span class="mr-2">🔒</span> 비밀번호 변경
          </h2>
          <p class="text-sm text-gray-600 mt-1">안전한 계정 보호를 위해 정기적으로 변경해주세요</p>
        </div>
    
        <%= form_with url: update_password_path, method: :patch, local: true, class: "space-y-4" do |f| %>
          <div class="space-y-4">
            <div>
              <label for="current_password" class="block text-sm font-medium text-gray-700 mb-1">
                현재 비밀번호
              </label>
              <input id="current_password" 
                     name="current_password" 
                     type="password" 
                     required 
                     class="appearance-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-lg focus:outline-none focus:ring-2 focus:ring-violet-500 focus:border-violet-500 sm:text-sm"
                     placeholder="현재 비밀번호 입력">
            </div>
            
            <div>
              <label for="password" class="block text-sm font-medium text-gray-700 mb-1">
                새 비밀번호
              </label>
              <input id="password" 
                     name="password" 
                     type="password" 
                     required 
                     class="appearance-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-lg focus:outline-none focus:ring-2 focus:ring-violet-500 focus:border-violet-500 sm:text-sm"
                     placeholder="새 비밀번호 (최소 6자)">
            </div>
            
            <div>
              <label for="password_confirmation" class="block text-sm font-medium text-gray-700 mb-1">
                새 비밀번호 확인
              </label>
              <input id="password_confirmation" 
                     name="password_confirmation" 
                     type="password" 
                     required 
                     class="appearance-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-lg focus:outline-none focus:ring-2 focus:ring-violet-500 focus:border-violet-500 sm:text-sm"
                     placeholder="새 비밀번호 다시 입력">
            </div>
          </div>
      
          <div class="px-2.5">
            <button type="submit" class="w-full flex justify-center py-3 px-6 border border-transparent text-sm font-medium rounded-lg text-white bg-violet-600 hover:bg-violet-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-violet-500 transition-colors cursor-pointer">
              비밀번호 변경
            </button>
          </div>
        <% end %>
      </div>
      
      <!-- 전화번호 변경 카드 -->
      <div class="bg-white p-8 rounded-xl shadow-lg">
        <div class="mb-6">
          <h2 class="text-xl font-bold text-gray-900 flex items-center">
            <span class="mr-2">📱</span> 전화번호 변경
          </h2>
          <p class="text-sm text-gray-600 mt-1">연락처 정보를 최신 상태로 유지해주세요</p>
        </div>
        
        <%= form_with url: update_phone_path, method: :patch, local: true, class: "space-y-4" do |f| %>
          <div class="space-y-4">
            <div>
              <label for="current_phone" class="block text-sm font-medium text-gray-700 mb-1">
                현재 전화번호
              </label>
              <div class="appearance-none relative block w-full px-3 py-2 border border-gray-200 bg-gray-50 text-gray-600 rounded-lg sm:text-sm">
                <% phone = @current_phone %>
                <% if phone.present? %>
                  <%= phone.gsub(/(\d{3})(\d{3,4})(\d{4})/, '\1-\2-\3') %>
                <% else %>
                  등록된 번호 없음
                <% end %>
              </div>
            </div>
            
            <div>
              <label for="new_phone" class="block text-sm font-medium text-gray-700 mb-1">
                새 전화번호
              </label>
              <input id="new_phone" 
                     name="phone" 
                     type="tel" 
                     required 
                     maxlength="11"
                     pattern="[0-9]{10,11}"
                     class="appearance-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-lg focus:outline-none focus:ring-2 focus:ring-violet-500 focus:border-violet-500 sm:text-sm"
                     placeholder="01012345678"
                     oninput="this.value = this.value.replace(/[^0-9]/g, '')">
              <p class="mt-1 text-xs text-gray-500">* 숫자만 입력 (하이픈 제외)</p>
            </div>
          </div>
          
          <div class="px-2.5">
            <button type="submit" class="w-full flex justify-center py-3 px-6 border border-transparent text-sm font-medium rounded-lg text-white bg-violet-600 hover:bg-violet-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-violet-500 transition-colors cursor-pointer">
              전화번호 변경
            </button>
          </div>
        <% end %>
      </div>
    </div>
    
    <!-- 메시지 표시 영역 -->
    <% if flash[:alert] %>
      <div class="mt-6 bg-red-50 border border-red-200 text-red-600 px-4 py-3 rounded-lg text-sm">
        ⚠️ <%= flash[:alert] %>
      </div>
    <% end %>
    
    <% if flash[:notice] %>
      <div class="mt-6 bg-green-50 border border-green-200 text-green-600 px-4 py-3 rounded-lg text-sm">
        ✅ <%= flash[:notice] %>
      </div>
    <% end %>
    
    <!-- 뒤로가기 버튼 -->
    <div class="mt-8 text-center">
      <%= link_to "← 서비스 메인으로", services_path, class: "inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-lg text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500 transition-colors" %>
    </div>
  </div>
</div>